# ── Hermes 官方源码（有独立 git repo）────────────────────────────
hermes-agent/

# ── 密钥和凭据 ──────────────────────────────────────────────────
.env
credentials/
db_workspace/

# ── 运行时状态 ──────────────────────────────────────────────────
state.db
state.db-shm
state.db-wal
kanban.db
kanban.db-shm
kanban.db-wal
verification_evidence.db
verification_evidence.db-shm
verification_evidence.db-wal
gateway.pid
gateway_state.json
auth.lock
feishu_seen_message_ids.json
channel_directory.json
people.yaml
people.draft.yaml
people.merged.yaml
people.yaml.bak
processes.json
.update_check
.tick.lock
.hermes_history
.clean_shutdown
.restart_last_processed.json
.restart_pending.json

# ── 密钥缓存（含明文 API key，绝对不入库）─────────────────────────
auth.json

# ── 运行时缓存 ──────────────────────────────────────────────────
__pycache__/
*.pyc
models_dev_cache.json
ollama_cloud_models_cache.json
.skills_prompt_snapshot.json
cache/
image_cache/
audio_cache/
state-snapshots/

# ── 日志和会话 ──────────────────────────────────────────────────
logs/
sessions/
cron/output/
cron/ticker_heartbeat
cron/ticker_last_success
cron/*.lock
sandboxes/

# ── 官方 Skills Hub 包（171MB，按需重装）─────────────────────────
skills/

# ── LSP server 沙箱（hermes lsp install 自动产物，含 node_modules 和带绝对路径的软链接）
lsp/

# ── Migration 临时数据 ──────────────────────────────────────────
migration/

# ── 平台 pairing 缓存 ───────────────────────────────────────────
platforms/

# ── 二进制文件 ──────────────────────────────────────────────────
bin/

# ── macOS ──────────────────────────────────────────────────────
.DS_Store
*.lock

# ── 项目缓存 ────────────────────────────────────────────────────
/tmp


# ── Obsidian ────────────────────────────────────────────────────
wiki/.obsidian/workspace.json
wiki/.obsidian/workspace-mobile.json
wiki/.obsidian/graph.json
wiki/.obsidian/core-plugins-galore.json
