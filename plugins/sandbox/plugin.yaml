name: sandbox
version: 0.2.0
description: "Per-chat Feishu boundary with read-only knowledge access, isolated group temp workspaces, and process-sandboxed trusted document scripts."
author: "周琛"
hooks:
  - pre_gateway_dispatch
  - pre_tool_call
